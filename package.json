{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "14.x",
    "npm": "7.x"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd client && npm run dev",
    "build-start": "concurrently \"cd backend && npm start\" \"cd client && npm start\"",
    "build": "concurrently \"cd backend && npm install\" \"cd client && npm install && npm run build\"",
    "start": "cd backend && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
